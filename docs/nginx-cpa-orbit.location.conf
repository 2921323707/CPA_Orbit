location = /cpa_orbit {
    return 301 /cpa_orbit/;
}

location ^~ /cpa_orbit/ {
    root /;
    try_files $uri $uri.html $uri/index.html =404;
    include /etc/nginx/snippets/mizuki-security-headers.conf;
    add_header Cache-Control "public, max-age=60, must-revalidate" always;
}
